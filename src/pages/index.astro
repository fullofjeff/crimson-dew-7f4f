---
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Coming Soon</title>
  <style>
    /* reset */
    html, body {
      margin: 0;
      padding: 0;
      width: 100%;
      height: 100%;
      overflow: hidden;
    }
    /* center & perspective */
    .card-container {
      display: flex;
      justify-content: center;
      align-items: center;
      width: 100%;
      height: 100%;
      perspective: 1000px;
      background: #f5f5f5; /* change if you want a solid‑color bg */
    }
    /* card sizing & 3D setup */
    .card {
      width: 80vw;
      max-width: 300px;
      aspect-ratio: 1597 / 1006; /* match your image’s natural ratio */
      position: relative;
      transform-style: preserve-3d;
      transition: transform 0.6s;
      cursor: pointer;
    }
    .card.is-flipped {
      transform: rotateY(180deg);
    }
    /* faces */
    .card-face {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      backface-visibility: hidden;
    }
    .card-face.back {
      transform: rotateY(180deg);
    }
    /* full‑bleed images */
    .card-bg {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }
  </style>
</head>
<body>
  <div class="card-container">
    <div class="card" id="card">
      <!-- FRONT -->
      <div class="card-face front">
        <img src="/card-front.webp" alt="Card Front" class="card-bg" />
      </div>
      <!-- BACK -->
      <div class="card-face back">
        <img src="/card-back.webp" alt="Card Back" class="card-bg" />
      </div>
    </div>
  </div>

  <script>
    const card = document.getElementById('card');
    card.addEventListener('click', () => {
      card.classList.toggle('is-flipped');
    });
  </script>
</body>
</html>
